"use strict";


// Контакт с пользователем

// const answers = [];

// answers[0] = prompt("Как ваше имя?", "");
// answers[1] = prompt("Как ваше фамилия?", "");
// answers[2] = prompt("Сколько вам лет?", "");

// console.log(answers);



// Интерполяция

// const user = "Mark";
// alert(`Привет, ${user}`);


// Операторы 

// let incr = 10,
//     decr = 10;

// ++incr; // Инкремент ++incr (Префиксной) incr++ (Постфиксной)
// --decr; // Декремент --decr (Префиксной) decr-- (Постфиксной)

// console.log(incr++);
// console.log(decr--);


const isChecked = true,
    isClose = true;

console.log(isChecked && isClose);